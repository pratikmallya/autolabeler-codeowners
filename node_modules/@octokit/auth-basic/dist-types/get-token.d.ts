import { AuthOptions, RequestInterface, State, TokenAuthentication } from "./types";
export declare function getToken(state: State, authOptions: AuthOptions, request?: RequestInterface): Promise<TokenAuthentication>;
